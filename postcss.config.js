module.exports = {
  // PostCSS plugins configuration
  // This ensures proper CSS processing and browser compatibility
  plugins: {
    // Tailwind CSS plugin for utility-first CSS framework
    // This generates utility classes based on your configuration
    tailwindcss: {},
    
    // Autoprefixer plugin for automatic vendor prefixing
    // This ensures CSS works across different browsers
    autoprefixer: {},
  },
}; 